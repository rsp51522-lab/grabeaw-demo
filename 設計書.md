# Grabeaw 見積書PDF自動作成機能　設計書

## 1. 前提条件・使用技術

対象は既存Apps Scriptプロジェクト「Grabeaw 商品注文・帳票管理システム」（V8ランタイム、タイムゾーン Asia/Tokyo）と、スプレッドシート「Grabeaw管理表」。PDF化はスプレッドシートの export URL（`format=pdf&gid=御見積書`）を `UrlFetchApp` + OAuthトークンで取得する既存方式を踏襲しています。A4縦・余白0.25インチ・グリッド線なし・シート名なし・ページ番号なしは既存 `buildSheetExportUrl_` の指定をそのまま利用します。

## 2. ファイル構成

### プロジェクト全体（導入後）

| ファイル | 状態 | 役割 |
|---|---|---|
| appsscript.json | 変更なし | 権限スコープ定義（Drive/Sheets/Forms/メール） |
| コード.gs | 変更なし | 定数（APP / SHEETS / HEADERS / SETTINGS / INPUT_CELLS / TEMPLATE_CELLS） |
| 無題.gs | 変更なし | 初期セットアップ（setupSystem 等） |
| **無題 2.gs** | **置き換え（1行追加）** | フォーム送信処理（onFormSubmit / processFormSubmit_） |
| 03_OrderService.gs | 変更なし | 注文番号発行・注文管理/明細への書き込み |
| 04_CustomerService.gs | 変更なし | 顧客の新規登録・既存更新（upsertCustomer_） |
| 05_DocumentService.gs | 変更なし | 帳票入力への読み込み・帳票シートへの転記 |
| 06_PdfService.gs | 変更なし | 手動メニュー用PDF作成 |
| 07_MailService.gs | 変更なし | 注文受付の管理者通知 |
| 08_Validation.gs | 変更なし | 入力チェック |
| 09_Utility.gs | 変更なし | 共通ユーティリティ |
| **10_Menu.gs** | 置き換え（任意・1行追加） | メニューに「見積書PDF自動作成テスト」を追加 |
| 11_ErrorLog.gs | 変更なし | エラーログ記録＋管理者通知 |
| **12_AutoEstimate.gs** | **新規追加** | 見積書PDF自動作成の全処理 |

## 3. 追加関数一覧（12_AutoEstimate.gs）

| 関数 | 役割 |
|---|---|
| autoCreateEstimateSafely_(orderNumber) | 安全版入口。失敗しても例外を投げず、エラーログ＋「PDF保存エラー」記録 |
| autoCreateEstimateForOrder_(orderNumber) | 本体。帳票反映→転記→PDF化→保存→URL書込→メール送信 |
| testSaveEstimate() | 手動テスト用。帳票入力の注文番号 or 最新注文で実行 |
| buildEstimateAutoFileName_() | 「会社名御中_御見積書_注文番号.pdf」等のファイル名生成 |
| getEstimateSaveFolder_() | 設定シートの見積書保存先フォルダ取得（PDF保存先へフォールバック） |
| trashSameNameFiles_() | 同名ファイルをゴミ箱へ移動（実質上書き。30日間復元可能） |
| writeEstimateUrlToFormResponses_() | フォーム回答「見積書PDF」列へURL書込（列が無ければ自動追加） |
| writeFormResponsePdfError_() | フォーム回答へ「PDF保存エラー」記録 |
| writeEstimateUrlToCustomers_() | 顧客管理「見積書PDF」列へURL書込＋更新日時更新 |
| findFormResponseRowByOrderNumber_() | 注文番号でフォーム回答の行を検索（新しい行から） |
| ensureColumnByHeader_() | 見出し列が無ければ末尾に追加 |
| sendEstimateMailToCustomer_() | 顧客向けPDF添付メール（テスト/本番モード切替） |
| sendEstimateAdminNotice_() | 管理者へ「〇〇様の見積書を保存しました」通知 |
| describeMailResult_() | メール送信結果の日本語化（通知文用） |
| ensureAutoEstimateSettings_() | 設定シートへ「メール送信モード」「顧客メール送信」を自動追加 |
| getLatestOrderNumber_() | 注文管理の最新注文番号取得（テスト用） |

## 4. データ設計（シートへの追加項目）

新しい列は3か所のみで、いずれも初回実行時にコードが自動追加します（手作業不要）。

| シート | 追加列 | 内容 |
|---|---|---|
| フォーム回答 | 見積書PDF（W列想定） | 保存したPDFのURL。失敗時は「PDF保存エラー」 |
| 顧客管理 | 見積書PDF（Q列想定） | 最新の見積書PDFのURL |
| 設定 | メール送信モード / 顧客メール送信 | 「テスト/本番」「ON/OFF」の2行 |

注文管理の「見積書PDF」列は既存のまま利用します（既存 `updateOrderPdfLink_` が「PDFを開く」リンクを設定）。

## 5. 処理シーケンス

```
onFormSubmit(e)                          … 無題 2.gs（既存＋1行追加）
 └ processFormSubmit_(e)
    ├ 注文登録・顧客upsert・受付通知     … すべて既存のまま
    └ autoCreateEstimateSafely_(注文番号) … ★追加行
       └ autoCreateEstimateForOrder_
          ├ ensureAutoEstimateSettings_   設定キー自動補完
          ├ loadOrderToInputSheet_        帳票入力へ反映（既存）
          ├ writeTemplateSheet_('estimate') 御見積書へ転記（既存）
          ├ UrlFetchApp → export URL      御見積書のみPDF化
          ├ trashSameNameFiles_ → createFile 保存（同名はゴミ箱へ）
          ├ updateOrderPdfLink_ ほか       3シートへURL書込
          ├ sendEstimateMailToCustomer_    顧客メール（モード判定）
          ├ sendEstimateAdminNotice_       管理者通知
          └ clearInputContents             帳票入力をクリア
```

## 6. エラー処理設計

エラーの伝播を2段階で止めます。第1段階として `autoCreateEstimateForOrder_` 内の失敗はすべて例外として投げ、第2段階の `autoCreateEstimateSafely_` が捕捉して、既存 `logError_`（日時・処理名・注文番号・エラー内容・スタックトレース・処理ユーザーをエラーログへ記録し、管理者へメール通知）を呼び、フォーム回答へ「PDF保存エラー」を書き込みます。これにより **PDF処理が失敗しても注文登録・顧客登録は必ず残ります**。

## 7. 既存コードレビュー結果（リファクタリング判断）

確認した範囲で二重処理・致命的な不具合はありませんでした。関数分割・定数管理・ヘッダー名ベースの列参照が既に徹底されているため、大規模リファクタリングはせず「追加ファイル方式」を採用しました（既存を書き換えるほどリスクが増えるため）。気づいた軽微な点は以下の3つです。

1. 設定シートの「管理者メールアドレス」が空欄のため、既存の受付通知・エラー通知が現在は送信されていない（入力すれば直る。コード修正不要）。
2. `getSettingValue_` が呼び出しごとに設定シートを全読みしている。現状の件数では実害なし。将来件数が増えたらキャッシュ化を検討。
3. 顧客管理の電話番号が `normalizePhone_` で数値化され先頭の0が落ちて見える（9081001417）。表示形式を「書式なしテキスト」にすれば解消。

## 8. 拡張ポイント（修正しやすい場所）

メール文面を変えたいときは `12_AutoEstimate.gs` 冒頭の `AUTO_ESTIMATE_MAIL_TEXT` だけを編集します。ファイル名規則は `buildEstimateAutoFileName_`、保存先は `getEstimateSaveFolder_`、列見出し名は `AUTO_ESTIMATE_HEADER` に集約してあります。請求書・領収書も同様に自動化したい場合は、`autoCreateEstimateForOrder_` の `'estimate'` を `'invoice'` / `'receipt'` に変えた関数を複製すれば同じ仕組みで動きます。
